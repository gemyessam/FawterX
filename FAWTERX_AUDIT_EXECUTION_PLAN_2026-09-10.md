# مراجعة FawterX وخطة التنفيذ تحت إشراف Codex

**التاريخ:** 10 سبتمبر 2026 — **الإصدار:** 2.27.65  
**HEAD المفحوص:** `6e854d0ac6a6e9696680950aeed8d04e2b1dc6bb`  
**الحالة:** مراجعة مركزة مكتملة؛ المراجعة التشغيلية الشاملة ما زالت تحتاج استكمالًا وفق جدول التغطية أدناه.

## 1. الهدف وحدود المسؤولية

Antigravity يراجع جميع أجزاء الموقع، يشخص المشكلات، ينفذ ويختبر. Codex يحدد الأولويات، يراجع القرارات والتغييرات الحساسة، ويفحص الأدلة مع تحقق مستقل محدد عند الحاجة. لا ينفذ Codex إصلاحات التطبيق ولا ينشره.

نتبع AGENTS.md: الأعمال الروتينية منخفضة المخاطر ينفذها Antigravity مباشرة ضمن تفويض المستخدم، ويجمع نتائجها في تقرير موجز. الأعمال المعقدة أو الحساسة تحتاج PLAN_APPROVED قبل التنفيذ وREVIEW_PASSED بعده. حساسية التغيير تحددها آثاره، لا عدد أسطره: تعديل صلاحية أو حذف أو استعادة بيانات ليس إصلاح واجهة بسيطًا.

هذه خطة العمل الحالية وتحديث للأولويات في FAWTERX_IMPROVEMENT_PLAN.md. لا تعِد تنفيذ بنود قديمة ثبت إصلاحها، ولا تعتبر هذه الوثيقة اعتمادًا مسبقًا لخطط الإصلاح الحساسة. قواعد الاختصار في ANTIGRAVITY_TOKEN_EFFICIENT_REVIEW_RULES.md مكملة لها.

## 2. ما راجعه Codex فعليًا

### تقدم مثبت

- مراجعة الفرق الإجمالي منذ مرجع 7 سبتمبر، ثم قراءة مركزة لصلاحيات المخزون والحفظ والمعاملات والاستعادة والتوقيع والأسرار.
- مسارا التسوية والصرف عبر الفواتير أصبحا يتضمنان فحوص الصلاحيات التي كانت مفقودة. لا يُعاد تسجيلهما كعيبين قديمين دون إعادة إنتاج جديدة.
- أضيفت طبقة معاملات للمخزون، تحقق من المدخلات والأرصدة، أرشفة للمشروعات، واستعادة عبر أجيال بيانات مع فحص checksum. لذلك توصيف الحفظ القديم على أنه دفعات منفصلة فقط لم يعد دقيقًا.
- اختبار مستقل: **54/54 backend** في 4 مجموعات، و**4/4 frontend**، وبناء الواجهة ناجح.
- حجم JavaScript الرئيسي في البناء الحالي: **1,866.67 KB**، و**524.05 KB gzip**؛ ما زال هناك تحذير حجم من Vite.

### مشكلات قائمة مؤكدة من الكود أو البناء

| ID | الملاحظة | دليل البداية | الأولوية |
| --- | --- | --- | --- |
| SEC-01 | مفتاح تشفير احتياطي ثابت، وفشل التشفير يعيد النص الأصلي؛ تعريض الحماية للضعف عند غياب الإعداد أو الفشل | backend/src/utils/cryptoUtil.js:4 و27 | عالية |
| SEC-02 | خدمة Signer تسمح بـCORS عام، ومسار التوقيع يحتاج ربطًا موثوقًا مع التطبيق | signer.cs:108 و131 | عالية |
| SEC-03 | أسرار إعدادات الربط تحفظ في localStorage، والمصادقة الثنائية معطلة في الواجهة الحالية | frontend/src/App.jsx:146 و209 و560 | عالية |
| PRIV-01 | نصوص تشغيل داخلية موجودة في ملف JavaScript العام رغم إخفائها بصريًا | frontend/src/components/ReleaseNotesModal.jsx؛ فحص bundle النهائي أعاد وجود النصوص | عالية إذا كان المقصود سريتها |
| PRIV-02 | كامل مستند ETA يطبع في سجل الخادم عند الإرسال | backend/src/routes/eta.js:186 | متوسطة |
| PERF-01 | تحميل حزمة رئيسية كبيرة بدل فصل الوظائف غير اللازمة للشاشة الحالية | نتيجة build الحالية | متوسطة |

### فجوات تحقق وليست أعطالًا مثبتة

- اختبارات المخزون تستخدم قاعدة وهمية مخصصة؛ runTransaction فيها يسلسل العمليات عبر Promise. هذا لا يثبت وحده سلوك retry والتنافس والقيود الفعلية في Firestore.
- كل حركة مخزون تنشئ نسخة استعادة تشمل عدة مجموعات داخل العملية؛ يلزم قياس التكلفة والحجم والزمن على بيانات اصطناعية كبيرة، وفحص سقف 450 كتابة وإرجاع خطأ مناسب دون أثر جزئي.
- لم تُجرَ معاينة تفاعلية كاملة للواجهة أو اختبار أدوار مسجلة الدخول في هذه الجولة.
- لم تُفحص قواعد Firestore المنشورة أو إعدادات الإنتاج الفعلية، ولم ينفذ إرسال ETA حقيقي أو توقيع USB أو تغيير بيانات إنتاج.
- سجل الإصدارات يذكر تحسينات للموبايل؛ ذلك تقرير تنفيذ، وليس بديلًا عن اختبار المقاسات والسيناريوهات الفعلية.

**أدلة الجولة:** C:/Users/GeMy/.codex/reviews/FawterX/audit-20260910/  
الملفات: backend-tests.log، frontend-tests.log، build.log، agy-result/result.json.

## 3. الخطوة الأولى عند Antigravity: استكمال مراجعة كل السطوح

هذه خطوة فحص فقط؛ اجمع النتائج مرة واحدة ولا ترسل طلب Codex لكل صفحة. اقرأ المرجع الحالي وأعد تصنيف البنود إن تغير الكود. استخدم حسابات وبيانات تجريبية معزولة؛ لا تنشئ عمليات فعلية على حسابات العملاء.

| السطح | سيناريوهات التغطية المطلوبة | الدليل المختصر |
| --- | --- | --- |
| الدخول والجلسات | دخول/خروج، انتهاء أو إلغاء التوكن، تبديل الحساب، جهاز جديد، تعليق الحساب | أدوار وحالات HTTP + عدم وصول غير مصرح للبيانات |
| الأدمن والعزل | المستخدم العادي مقابل الأدمن، صلاحيات مشروع A/B، الوصول بالروابط المباشرة، الاستعادة والحذف | جدول العملية × الدور؛ اختبار منع قبل الكتابة |
| إعدادات الشركة | حفظ/تعديل/فشل الاتصال، حسابان منفصلان، انتقال الأسرار القديمة | مصدر وحركة السر دون طباعة قيمه |
| الاستيراد | Excel/CSV/PDF المدعوم، ملفات تالفة أو كبيرة، حقول ناقصة، عربي، أصفار بادئة، BAR/LM/KG | عينات اصطناعية صغيرة + المتوقع/الفعلي |
| حساب الفاتورة | خصم/ضريبة/تقريب/تاريخ/وحدة/عملة وحالات حدودية | مثال رقمي مرجعي واختبار مناسب؛ تحقق التزامات ETA من المصدر الرسمي عند الحاجة |
| التوقيع والإرسال | تطابق المستند الموقع والمرسل، فشل الجهاز، timeout، إعادة المحاولة، رفض أو نجاح جزئي | ETA وهمية وSigner بديل للاختبارات، مع حدود التحقق |
| المسودات والمتابعة | حفظ وفتح وتعديل وإرسال، حالة استلام الطلب مقابل النتيجة النهائية، عداد الاستهلاك مع الدفعات | جدول انتقال الحالة ومعرفات العمليات |
| المخزون | توريد/صرف/تكرار طلب/صرف متزامن/إلغاء/رصيد غير كاف | اختبارات محاكي Firestore؛ فصل أثر كل مشروع |
| الدهان والتسليم | تسليم جزئي، مخزون خارجي، إلغاء مرتبط، منع إحياء الملغى | كميات قبل/بعد وروابط المستندات |
| الأرشفة والاستعادة | بيانات متعددة المجموعات، snapshot تالف، فشل staging، تعديل متزامن، بيانات كبيرة | الرصيد والجيل النشط قبل/بعد وعدم الاستبدال عند الفشل |
| تجربة المستخدم | 360 و768 و1440 بكسل، RTL/English، تنقل لوحة مفاتيح، مودالات وجداول ورسائل الخطأ والفراغ | لقطة لكل خلل فقط؛ ذكر الشاشة/المقاس/الدور |
| الأداء والخصوصية والتشغيل | تحميل أولي، استعلامات متكررة وكبيرة، bundle، logs، headers/CORS، dependencies، SEO وقابلية الاسترجاع | قياس وخطوات إعادة إنتاج؛ لا تغيير إعدادات الإنتاج لمجرد الفحص |

لكل صف ضع: CHECKED / ISSUE / NOT_CHECKED، نطاق الفحص، مرجع الدليل، ومعرف المشكلة إن وجدت. CHECKED يعني السيناريوهات المذكورة فقط ولا يعني أن السطح خالٍ من كل العيوب. لا تطلب من Codex استكشاف المشروع من الصفر لتعويض خانة ناقصة؛ اذكر العائق تحديدًا.

## 4. سجل إشراف واحد بدل إعادة إرسال السياق

أنشئ ملف audit-index.md خارج كود التطبيق داخل مجلد مراجعة مؤرخ. يحتوي:

```text
BASE: HEAD + dirty baseline
COVERAGE: السطح | الحالة | ما فُحص | evidence path
TASKS: ID | المشكلة/الدليل | الخطورة | الخطة | الحالة | effort | review reference
OPEN_DECISIONS: القرارات المطلوب حسمها فقط
CHECKPOINT: ما تغير منذ آخر تقرير
```

حالات الطلب: NEW → DIAGNOSED → PLAN_REVIEW → IMPLEMENTING → DIFF_REVIEW → DONE. للأعمال البسيطة احذف خطوتي المراجعة، مع إبقاء الأدلة. DONE يتطلب شروط القبول، وقرار Codex أيضًا للطلبات الحساسة.

سجل كل جزء حتى لو لم تكن به مشكلة. بذلك تشمل المتابعة كل الموقع دون استدعاء مراجع لكل تعديل تجميلي. الملفات والlogs واللقطات تحفظ مرة واحدة؛ التقارير اللاحقة ترسل مساراتها والفروق فقط. لا تنشئ dashboard أو نظام إدارة مهام جديدًا لهذا الغرض.

## 5. ترتيب التنفيذ بعد تقرير التغطية

| الدفعة | العمل | معيار القبول | Codex |
| --- | --- | --- | --- |
| A1 | SEC-01: منع fallback غير الآمن والحفظ الصريح مع خطة انتقال للأسرار | غياب المفتاح وفشل التشفير يمنعان الحفظ؛ قراءة البيانات القديمة واختبار الرجوع دون فقد إعدادات | High: خطة + diff |
| A2 | SEC-02: ربط Signer وتقييد الطلبات والمصادر | طلب غير مربوط لا يصل للتوقيع؛ سيناريو مسموح ناجح ببديل محلي؛ توافق نسخة البرنامج واضح | High: خطة + diff |
| A3 | SEC-03: تقليل الأسرار في المتصفح وخطة 2FA متكاملة | عزل الحسابات وحذف النسخ القديمة بعد الانتقال؛ لا تفعيل دون إنفاذ خادمي واسترداد وصول | High؛ قسمها لطلبين إذا اختلف النطاق |
| A4 | PRIV-01/02: المحتوى الداخلي وتنقية السجلات | محتوى محدد سريًا لا يوجد بأي أصل عام أو source map؛ API محمي؛ لا فواتير كاملة في logs | High للحدود الأمنية؛ Medium لتنقية logs |
| B1 | إثبات سلامة المخزون الجديد بمحاكي Firestore وقياس أحجام واقعية | retry/تنافس/استعادة متزامنة/حدود العملية لا تترك أثرًا جزئيًا؛ تقرير استعلامات وحجم وزمن | High قبل أي تعديل إنتاجي |
| B2 | دورة الفاتورة والإرسال والاستهلاك وفق نتائج الفحص | نتيجة مجهولة لا تعامل كفشل قطعي؛ دفعات جزئية واضحة؛ عداد متسق؛ لا ازدواج أثر عند retry | High للحسابات أو ضمانات الإرسال |
| C1 | parsing والتحقق من المدخلات التي ثبت فشلها | اختبار صغير يعيد الخطأ ويثبت الإصلاح دون تغيير سلوك القوالب الأخرى | Medium؛ High إذا مس الحساب أو المستند الموقع |
| C2 | التحميل التدريجي والاستعلامات المحدودة وتقسيم الملفات | قياس قبل/بعد لنفس البيئة؛ تحميل الشاشة لا يستدعي الوظائف غير اللازمة؛ لا فقد للسلوك | Medium لإعادة التنظيم المتشعبة |
| D | عيوب UI/RTL/mobile/نصوص/SEO المؤكدة | معاينة المقاس والدور المعني + فحص مناسب؛ تقرير مجمع | تنفيذ مباشر إذا منخفضة المخاطر |

هذه أولويات قابلة للتحديث بالدليل؛ العطل الذي يمنع العمل أو يعرض البيانات للخطر يتقدم. لا تصلح شيئًا لمجرد وجوده في الخطة القديمة. لا تخلط إصلاح أمني مع إعادة تنسيق واسعة أو نقل ملفات في نفس diff.

## 6. أقل دورة تواصل فعالة عبر delegate-skills

1. بعد اكتمال جدول التغطية، أرسل **تقريرًا إجماليًا واحدًا** 250–400 كلمة، ومعه مسار audit-index. يراجع Codex ترتيب المخاطر والنواقص، ولا يعيد قراءة جميع السطوح تلقائيًا.
2. لكل طلب حساس: brief خطة 120–250 كلمة → PLAN_APPROVED → تنفيذ → brief diff 120–250 كلمة مع الأدلة → قرار. اجمع التعديلات المرتبطة التي لها نفس معيار القبول.
3. المتابعة FIX تكون 50–120 كلمة: ملاحظة → تصحيح → اختبار، مع delta منذ المراجعة السابقة. لا تعِد إرسال الملفات أو الخلفية المستقرة.
4. الأعمال البسيطة تُلخص عند انتهاء دفعة متماسكة؛ لا review لكل نص أو زر. Codex يفحص عينة محددة من الأدلة عند checkpoint أو خلل ظاهر، ولا تتحول العينة إلى اعتماد شامل لكل صفحة.
5. أرسل عند اكتمال الدفعات تقرير تغطية نهائيًا: ما أُغلق، ما بقي، والقيود. المطلوب إشراف شامل بالاستثناءات والأدلة، وليس تكرار كل خطوة تنفيذ.

اختيار المراجع: **gpt-6-astra Low** للفرز ومتابعات صغيرة، **Medium** للمنطق المعتاد والتحليل المجمع، **High** للصلاحيات والتشفير والتوقيع والحسابات والتزامن والترحيل. اختر أقل درجة كافية من البداية، ولا تستدعِ الثلاث درجات تباعًا. بعض الأعمال الروتينية لا تحتاج استدعاء أصلًا.

```text
TYPE: AUDIT_SUMMARY | PLAN | DIFF | FIX | CHECKPOINT
ID: <معرف ثابت>
EFFORT: low/medium/high | WHY: <سبب في سطر>
BASE: <HEAD + dirty baseline + نسخة diff>
ASSESSMENT: <المشكلة، السبب ودليله، والأثر>
ASK: <القرار المطلوب تحديدًا>
CHANGE: <الخطة/التغيير في نقاط قليلة>
FILES: <مسارات مطلقة + الدوال المهمة + الملفات الجديدة>
EVIDENCE: <الأوامر، exit codes، النتائج، مسارات التفاصيل>
UNKNOWN: <الحدود إن وجدت>
INDEX: <مسار السجل؛ في المتابعة ما تغير فقط>
```

رد Codex: VERDICT + النسخة المراجعة + الملاحظات الجوهرية + VERIFIED مستقلًا + REPORTED عن المنفذ + NEXT. مراجعة التقرير العام لا تمنح PLAN_APPROVED لكل طلب داخله.

### أمر الاستدعاء

مثال: استبدل placeholders بمسارات فعلية، واختر effort للطلب. --read-only إلزامي لكل مراجعة بما فيها الاستئناف:

```powershell
node 'C:/Users/GeMy/.gemini/antigravity/skills/codex-delegate/scripts/relay.mjs' --brief '<absolute-brief-path>' --cd 'C:/Users/GeMy/.gemini/antigravity/scratch/FawterX' --read-only --ignore-user-config --model gpt-6-astra --effort medium --out-dir '<unique-output-directory>' --timeout 20m
```

استأنف threadId المحدد عبر --session؛ تجنب --resume-last العام. لا تكرر الطلب قبل التأكد من انتهاء السابق. لا تستخدم تفويضًا متداخلًا: Codex يرد للمنفذ ولا يشغل Antigravity من داخل جلسة المراجعة. اقرأ result.json والرد الكامل؛ نجاح relay أو غياب الرد ليس اعتمادًا.

هذه جلسات CLI منفصلة، ولا توقظ مهمة Codex المفتوحة تلقائيًا. للاطلاع من المهمة الحالية، يرسل المستخدم مسار audit-index أو آخر result.json. لا يُدّعى وجود متابعة خلفية مستمرة لمجرد تثبيت المهارة.

## 7. فحوص محددة بدل مضاعفة التنفيذ

- يسجل Antigravity الفحوص المناسبة للنسخة المسلمة. Codex يقرأ diff وتغطية الاختبار ولا يعيد build/tests تلقائيًا لكل متابعة.
- يعيد Codex أصغر تحقق مستقل عند خطر حساس أو تعارض أو دليل قديم أو حالة ناقصة. لا يتنازل عن قراءة التغييرات الفعلية مقابل وعد المنفذ بأنها صحيحة.
- تشغل البوابات الكاملة مرة للدفعة المناسبة وعند تغيير مؤثر أو فشل جديد؛ لا تعاد دون سبب على نفس النسخة.
- البوابات الحالية: npm --prefix backend test -- --runInBand --silent، وnode --test frontend/tests/warehouseCoating.test.mjs، وnpm --prefix frontend run build.
- لا تعتبر نجاح unit tests على fake إثباتًا لسلوك خدمة الإنتاج. اختبارات Firestore تكون في محاكي معزول، وETA/Signer ببدائل محلية. اختبار حي يتطلب نطاقًا مقصودًا ومصرحًا به.
- احفظ logs خارج المشروع وأرسل ملخصها فقط. لا تضع محتوى invoice أو مفاتيح في brief أو screenshots.

## 8. نقطة البدء والحالة العملية

ابدأ **بالفحص وفق القسم 3 وتقرير مجمع واحد**، ثم قدم خطة A1 التفصيلية. لا تنفذ تغييرات حساسة قبل مراجعتها، ولا تنتظر Codex لكل تحسين شكلي بسيط مصرح به.

تمت محاولة جمع مراجعة إضافية عبر agy-delegate بتاريخ 10 سبتمبر: الخدمة رفضت الطلب بسبب حصة Antigravity CLI، وذكرت تجديدًا بعد نحو 11 ساعة و17 دقيقة وقت المحاولة. لم ينتج تقرير مراجعة من Antigravity، ولم تُكرر المحاولة. يمكنه العمل من جلسته المتاحة وإرسال التقارير عبر codex-delegate إذا سمحت حصصه، أو ينقل المستخدم ملفات المراجعة. لا تبدل حسابًا أو موديلًا لتجاوز الحصة تلقائيًا.

المجلد unity_card_assets/ كان غير متتبع قبل المراجعة وخارج نطاقها؛ لا تحذفه ولا تضمه لأي تغيير تلقائيًا. لم يُغير Codex كود التطبيق ولم ينفذ commit أو نشرًا في هذه الجولة.
