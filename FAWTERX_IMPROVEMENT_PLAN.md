<div dir="rtl" align="right">

# خطة تحسين FawterX — تنفيذ Antigravity ومراجعة Codex

**التاريخ:** 7 سبتمبر 2026  
**الإصدار الذي تمت مراجعته:** `2.27.59`  
**مرجع Git وقت إعداد الخطة:** `f6fa6859826a515f09a1bef612f680e8729c8b75`  
**الحالة:** خارطة تحسين جاهزة ليحوّلها Antigravity إلى خطط تنفيذ مرحلية تُراجع قبل تعديل الكود.

## 1. الهدف وفهم المنتج

FawterX منصة لتجهيز الفواتير الإلكترونية المصرية من الملفات، ومراجعتها وتوقيعها محليًا وإرسالها، مع وحدة مخزون ومشروعات تشمل التوريد والصرف والدهان والتسليم. تفاصيل وحدات الأعواد والأمتار والأوزان والألوان وأكواد الموردين تجعل وحدة المخزون متخصصة في تشغيل قطاعات الألومنيوم.

الهدف هو زيادة موثوقية العمليات وحماية بيانات العملاء، ثم تسهيل الاستخدام وتحسين الأداء. نحافظ على الوظائف الحالية ونحسنها تدريجيًا؛ لا تتطلب هذه الخطة إعادة كتابة المشروع أو تغيير قاعدة البيانات أو إطار الواجهة بالكامل.

## 2. اتفاق التنفيذ والمراجعة

- **Antigravity:** يفحص المشكلة، يكتب الخطة التفصيلية، ينفذ بعد اعتمادها، يضيف اختبارات الانحدار المناسبة، ويصلح الملاحظات.
- **Codex:** يراجع الخطة والكود والنتائج، ويعيد تشغيل الفحوص المناسبة مستقلًا. لا ينفذ إصلاحات التطبيق ولا يعمل commit أو push أو deploy.
- يقرأ الطرفان `AGENTS.md` في جذر المشروع. تعليماته هي مرجع دورة العمل.
- هذه الوثيقة ليست اعتمادًا مسبقًا لكل القرارات التقنية أو إذنًا بتنفيذ كل المراحل دفعة واحدة.
- يبدأ التنفيذ بطلب واحد محدد، ثم `PLAN_APPROVED`، ثم التنفيذ، ثم `REVIEW_PASSED` على التغييرات نفسها. عند وجود ملاحظات تكون النتيجة `CHANGES_REQUESTED`.
- فشل التواصل أو انتهاء الحصة أو غياب الرد لا يعتبر موافقة. يمكن نقل طلب المراجعة ونتيجتها عبر المستخدم إلى مهمة Codex الحالية عند تعذر CLI.
- لا يبدأ النشر قبل اجتياز مراجعة التنفيذ، ويظل ضمن تفويض المستخدم للنشر. أي تعديل كود بعد المراجعة يعاد فحصه.

## 3. ما تم التحقق منه وما لم يتم

### تم التحقق

- بناء الواجهة نجح، ونجحت الاختبارات الستة الموجودة في مجموعتين وقت المراجعة.
- اختبار محلي معزول لمسارات المخزون، دون اتصال بقاعدة الإنتاج، أثبت وصول مستخدم عرض فقط إلى خدمة تسوية المخزون والتكاليف.
- الاختبار نفسه أثبت وصول مستخدم يملك الرفع وممنوع من الصرف إلى خدمة معالجة فاتورة `outbound` عبر مسار استيراد الفواتير.
- الكود يحفظ مستند الفاتورة قبل حفظ جميع حركاتها، ويقسم الحركات والأرصدة إلى دفعات منفصلة.
- خدمة التوقيع المحلية تستخدم `Access-Control-Allow-Origin: *` دون تحقق ظاهر من ربط جلسة التطبيق بطلب التوقيع.
- يوجد مفتاح تشفير احتياطي ثابت، ومسار خطأ يعيد النص الأصلي عند فشل التشفير. أسرار الاتصال محفوظة أيضًا في `localStorage`، والمصادقة الثنائية معطلة في الكود الحالي.
- نصوص من سجل التحديثات الداخلي موجودة داخل حزمة JavaScript النهائية التي ينزلها المتصفح، رغم إخفائها عن بعض المستخدمين.
- حجم حزمة JavaScript في البناء المقاس نحو 1.86 MB قبل الضغط و521 KB بعد gzip. صفحة المخزون نحو 6916 سطرًا والرئيسية نحو 2761 سطرًا.

### لم يتم التحقق

- لم يتم إرسال فاتورة حقيقية أو استخدام USB Token أو إجراء تغييرات على Firestore الإنتاجية.
- لم يتم تدقيق إعدادات الإنتاج الفعلية أو قواعد Firestore المنشورة أو إجراء اختبار اختراق شامل.
- مخاطر التزامن والفشل الجزئي مستنتجة من بنية الكود؛ يلزم إثبات حالات الانحدار في بيئة معزولة أثناء التنفيذ.
- اقتراحات تجربة الاستخدام أدناه تحتاج معاينة الواجهة أثناء التشغيل ببيانات تجريبية؛ لا تمثل نتائج اختبار مستخدمين.

> على Antigravity إعادة التأكد من كل موضع في الكود الحالي؛ أرقام الأسطر والإصدار المذكوران مرجع للمراجعة السابقة وليسا بديلًا عن الفحص.

## 4. ترتيب المراحل

| المرحلة | الأولوية | الناتج المطلوب |
| --- | --- | --- |
| 1 | عاجلة | إغلاق تجاوزات صلاحيات المخزون |
| 2 | عاجلة | حماية الأسرار وخدمة التوقيع والبيانات الداخلية |
| 3 | عالية | منع تكرار وازدواج أو تجزئة أثر حركات المخزون |
| 4 | عالية | ضبط دورة الفاتورة والتوقيع ومتابعة الإرسال |
| 5 | متوسطة | تقسيم الكود وتحسين التحميل والاستعلامات |
| 6 | متوسطة | تحسين تجربة المستخدم وتسليم إصدار موثق |

كل مرحلة تتقسم إلى طلبات صغيرة قابلة للمراجعة. يفضل فصل التوقيع عن التشفير وعن سجل التحديثات، وفصل تغيير الأرصدة عن إعادة تنظيم الواجهة.

## 5. المرحلة الأولى: صلاحيات المخزون

**المواضع الأساسية:** `backend/src/routes/warehouse.js`، `backend/src/services/warehouseStore.js`، واختبارات API جديدة.

### المطلوب

1. إعداد جدول لكل مسارات المخزون: العملية، صلاحية المشروع، الدور، والأعلام المطلوبة مثل `canUpload` و`canEdit` و`canDispatch`.
2. تقييد `POST /projects/:projectId/reconcile-delmar-and-costs` بصلاحية تعديل مناسبة، ومنع `warehouse_viewer` منه.
3. في `POST /projects/:projectId/invoices/process`، تطبيق صلاحية الصرف أيضًا عندما تكون الحركة `outbound`. التحقق يكون بعد توحيد قيمة نوع الحركة والتحقق من صحتها.
4. توحيد تطبيق الصلاحيات في الخادم بحيث لا يمكن الوصول لنفس الأثر عبر مسار أقل حماية.
5. مراجعة المشاريع البديلة مثل `default_canex`: يتم فحص الصلاحية على المشروع الفعلي بعد حل الاسم البديل.
6. مراجعة القيم الافتراضية للصلاحيات القديمة؛ أي تشديد يحتاج ترحيلًا واضحًا يحافظ على وصول المستخدمين المصرح لهم.
7. فحص عمليات الاستعادة والحذف والربط والتسوية للتأكد من أنها تتبع جدول الصلاحيات نفسه.

### شروط القبول

- مستخدم العرض فقط يحصل على `403` لكل عملية كتابة، ولا يتم استدعاء خدمة التعديل أصلًا.
- `canDispatch=false` يمنع الصرف اليدوي والصرف عبر الفواتير، بما في ذلك اختلاف حالة الأحرف أو الإدخال غير الصالح.
- المستخدم لا يقرأ أو يعدل مشروعًا خارج صلاحياته، حتى باستخدام اسم بديل.
- المستخدم المصرح له يظل قادرًا على إتمام العملية الصحيحة.
- اختبارات التكامل تتحقق من النتيجة وعدم حدوث أي كتابة عند الرفض، باستخدام بدائل محلية أو محاكي قاعدة البيانات.

## 6. المرحلة الثانية: الأسرار والتوقيع والخصوصية

### 2-A — إدارة الأسرار

**المواضع:** `backend/src/utils/cryptoUtil.js`، `backend/src/services/userStatsStore.js`، مسارات الإعدادات، `frontend/src/App.jsx`، `frontend/src/services/api.js`.

- جعل مفتاح التشفير إعدادًا إلزاميًا في الإنتاج، مع فشل واضح عند غيابه بدل الاعتماد على قيمة ثابتة.
- عند فشل التشفير، رفض الحفظ؛ لا يسمح بحفظ السر كنص صريح أو تجاهل الخطأ.
- تصميم انتقال للبيانات القديمة قبل تغيير المفتاح: تحديد شكل البيانات، طريقة إعادة التشفير، النسخ الاحتياطي الآمن، وكيفية الرجوع. لا تستبدل المفتاح ثم تكتشف فقدان القدرة على قراءة الإعدادات.
- تقليل إرسال الأسرار إلى المتصفح: الخادم يستخدم الإعدادات المحفوظة، والواجهة تستقبل حالة الاتصال وقيمًا مقنعة عند الإمكان.
- إزالة النسخ القديمة من `localStorage` بعد اكتمال الانتقال، ومعالجة تسجيل الخروج وتبديل الحساب.
- تنقية سجلات التشغيل من كامل الفواتير والأسرار، وإظهار تفاصيل الخطأ الفنية في سجلات منقحة بدل إعادتها للمستخدم.

**القبول:** فشل التشفير يمنع الحفظ، الحسابات تظل معزولة، الإعدادات القديمة تظل قابلة للاستعادة، ولا تظهر الأسرار في تخزين المتصفح المستهدف أو الردود أو السجلات بعد الانتقال.

### 2-B — خدمة التوقيع المحلية

**المواضع:** `signer.cs`، `frontend/src/utils/signEtaDocuments.js`، `frontend/src/services/api.js`، وطريقة توزيع برنامج التوقيع.

- تصميم ربط واضح بين جلسة التطبيق وبرنامج التوقيع، مع رمز قصير العمر وحدود استخدام مناسبة.
- فحص المصدر بدقة وتحديد المصادر المسموح بها. CORS وحده ليس مصادقة، وغياب `Origin` لا يمنح طلب التوقيع إذنًا تلقائيًا.
- ضبط حجم الطلب والمهلة وتزامن طلبات التوقيع ورسائل فشل الجهاز.
- عدم تغيير خوارزمية التوقيع أو طريقة إعداد المستند لمجرد تحسين النقل أو الحماية.
- توثيق توافق نسخة الواجهة ونسخة Signer، وخطة التحديث إذا احتاج المستخدم تنزيل نسخة جديدة.

**القبول:** الطلب دون ربط صالح أو من مصدر مرفوض لا يصل لعملية التوقيع، بينما ينجح السيناريو المسموح باختبارات محلية دون توكن حقيقي. اختبار الجهاز الفعلي خطوة مستقلة يُذكر بوضوح إن لم تنفذ.

### 2-C — المحتوى الداخلي والمصادقة الثنائية

**المواضع:** `frontend/src/components/ReleaseNotesModal.jsx`، ملفات الواجهة ذات المحتوى الداخلي، `backend/src/middleware/deviceAuth.js`، `backend/src/routes/authSecurity.js`، ومكون المصادقة الثنائية.

- فصل المحتوى الداخلي المقصود اعتباره سريًا عن الملفات العامة؛ جلبه من مسار خادم يتحقق من الصلاحية. تقسيمه في chunk آخر متاح للعامة لا يحميه.
- مراجعة النصوص الأخرى المضمنة في صفحات المخزون لاكتشاف تفاصيل تشغيل فعلية لا ينبغي تضمينها في ملفات عامة.
- تقديم خطة مستقلة لإعادة تفعيل 2FA: إعداد العامل الثاني، تحدي الجهاز الجديد، إنفاذ التحقق على الخادم، واسترداد الوصول. لا يكتفى بإظهار نافذة في الواجهة ولا يفعل النظام دون مسار استرداد.

**القبول:** فحص كل ملفات البناء العامة، بما فيها source maps إن وجدت، لا يجد النصوص الداخلية المحددة؛ API يرفض غير المصرح له؛ وخطة 2FA لها اختبارات منع التجاوز والتعافي قبل تفعيلها.

## 7. المرحلة الثالثة: سلامة المخزون والحركات

**المواضع:** `backend/src/services/warehouseStore.js`، مسارات الحركة والاستعادة، واختبارات محاكي Firestore.

### المطلوب

1. تعريف مصدر الحقيقة: سجل الحركات، وطريقة اشتقاق الرصيد منه، وقواعد الإلغاء والحركات المحذوفة والدهان والمخازن الخارجية.
2. إضافة معرف ثابت للعملية يمنع تكرار أثر نفس الطلب عند إعادة الإرسال أو انقطاع الاتصال. رقم الفاتورة وحده يحتاج مراعاة المورد والنوع والمشروع.
3. تصميم حفظ موثوق للمستند والحركات والأرصدة. العمليات الصغيرة قد تستخدم معاملة ذرية؛ العمليات الكبيرة تحتاج حالات تنفيذ واستئناف واضحة. لا تفترض أن تقسيم الكتابات إلى دفعات يحقق ذرية العملية كلها.
4. إظهار العملية كمكتملة فقط بعد اكتمال أثرها، مع منع تكرار الخصم أو الإضافة أثناء إعادة المحاولة.
5. التحقق على الخادم من القيم الرقمية والمنتهية ووحدات القياس واتجاه الحركة. توضيح سياسة المخزون السالب واعتماد الاستثناءات التجارية قبل فرضها.
6. حماية قرارات حجز/صرف الرصيد من طلبين متزامنين. الزيادة الذرية وحدها لا تضمن كفاية الرصيد.
7. مراجعة الإلغاء والاسترجاع لمنع تطبيق نفس العكس مرتين ولتحديد أثره على التسليمات اللاحقة.
8. إخراج إصلاح الأرصدة التلقائي من مسار قراءة المخزون إلى عملية صريحة بصلاحية وسجل تدقيق. مراجعة منطق اشتقاق الرصيد للأصناف المحذوفة والحركات الملغاة والتغطية من مخزن خارجي.

### شروط القبول

- إرسال نفس العملية مرتين، حتى بالتزامن، يحدث أثرًا واحدًا فقط.
- انقطاع التنفيذ بعد إحدى الدفعات لا يترك عملية تظهر كمكتملة بحركات ناقصة، ويمكن استئنافها أو التراجع عنها وفق التصميم المعتمد.
- طلبا صرف متزامنان لا يخالفان سياسة الرصيد المعتمدة.
- إلغاء العملية مرتين لا يعيد الكمية مرتين.
- الكميات السالبة أو غير الرقمية أو نوع الحركة غير المعروف ترفض بوضوح وفق قواعد الإدخال.
- القراءة لا تغير بيانات المخزون، وإعادة بناء الرصيد تتعامل بشكل صحيح مع الإلغاء والتغطية الخارجية.
- توجد خطة ترحيل وفحص بيانات تجريبية وخطة رجوع قبل تطبيق أي تغيير بنيوي على البيانات الحقيقية.

## 8. المرحلة الرابعة: موثوقية دورة الفاتورة

**المواضع:** `backend/src/routes/eta.js`، خدمات الإرسال والمسودات والاستهلاك، `backend/src/utils/etaMapper.js`، `backend/src/utils/etaValidator.js`، `frontend/src/utils/signEtaDocuments.js`، ومسارات الإرسال في الواجهة.

هذه مرحلة تحقق وتحسين؛ لا تعني أن كل حالة أدناه ثبت فشلها حاليًا.

- تتبع مسارات الإرسال من الرئيسية والمسودات والدفعات والتأكد من استخدام نفس التنفيذ الفعلي لتجهيز المستند والتوقيع. لا تحذف الدوال المتشابهة قبل التأكد من استخدامها.
- فصل حالات: مسودة، جاهزة، موقعة، جارٍ الإرسال، تم استلام الطلب، نتيجة نهائية، وفشل اتصال بنتيجة غير معروفة. أي ادعاء عن حالات ETA أو التوقيع يتحقق من توثيق ETA الرسمي أثناء التنفيذ.
- عند مهلة اتصال، الاستعلام عن العملية المعروفة قبل إعادة الإرسال متى سمحت الواجهة الرسمية بذلك؛ لا تعتبر timeout دليلًا على أن الجهة لم تستلم الطلب.
- عرض النتائج على مستوى الفاتورة عند نجاح بعض عناصر الدفعة ورفض الباقي، وحفظ المعرفات اللازمة للمتابعة.
- مراجعة عداد الاستهلاك تحت التزامن ومع الدفعات؛ تعريف هل العد لكل فاتورة أم لكل طلب وتطبيقه بطريقة متسقة.
- تثبيت قواعد الحساب والتقريب والوحدات والتواريخ بأمثلة مرجعية مجهولة البيانات. عدم تغيير التنسيق الذي يدخل في التوقيع دون اختبارات مطابقة.

**القبول:** اختبارات باستخدام ETA وهمية تغطي الرفض والمهلة والنجاح الجزئي وإعادة المحاولة؛ لا يعاد إرسال فاتورة معروفة النجاح تلقائيًا؛ القيم قبل التوقيع والإرسال متطابقة؛ وتظهر للمستخدم حالة دقيقة يمكن متابعتها.

## 9. المرحلة الخامسة: تنظيم الكود والأداء

**المواضع:** `frontend/src/pages/Warehouse.jsx`، `frontend/src/pages/Home.jsx`، `frontend/src/App.jsx`، خدمات المخزون والتحليل، وإعدادات البناء والاستضافة.

- التقسيم تدريجي حسب الوظيفة: جداول المخزون، مراجعة الاستيراد، الحركة اليدوية، التسليمات، الاستعادة، ومنطق البيانات.
- نقل منطق الحساب والتحقق القابل للاختبار إلى وحدات مستقلة. تجنب خلط إعادة التنظيم مع تغيير قواعد المخزون في نفس الطلب.
- تحميل الصفحات والمكتبات الثقيلة عند الحاجة، مع حالات تحميل وأخطاء واضحة.
- إضافة pagination وفلاتر على الخادم للاستعلامات الكبيرة بدل تحميل التاريخ الكامل عند كل فتح، مع فحص الفهارس والتكلفة اللازمة.
- مراجعة React Hooks ومعالجة الأخطاء وتنظيف الدوال غير المستخدمة وتوحيد الأنماط المتكررة.
- مراجعة الاعتماديات وسكربتات التصحيح القديمة، وتوثيق التشغيل والإعدادات في README. لا يتم تحديث شامل للمكتبات أو حذف ملفات تاريخية دون تحديد أثره.

**القبول:** جميع السيناريوهات المتأثرة تعمل كما كانت؛ الدخول لا يحمل كود المخزون غير اللازم؛ قياس حجم التحميل الأولي ووقت العرض قبل/بعد على نفس البيئة؛ وعدم تقديم تحسن الحجم كدليل على حماية المحتوى السري.

## 10. المرحلة السادسة: تجربة المستخدم والتسليم

- شاشة بداية تعرض العمل المطلوب: مسودات تحتاج تصحيحًا، إرسالًا يحتاج متابعة، وشحنات متأخرة لمن يملك صلاحيتها.
- شاشة استيراد توضح مصدر القيمة المستخرجة، والحقول المشكوك فيها، وأثر تعديلها على الإجمالي. تمييز الثقة لا يعني اختراع نسبة دقة غير مقاسة.
- فصل التنقل بصريًا بين الفواتير والمخزون، مع إبقاء الصلاحيات مفروضة في الخادم.
- رسائل أخطاء عربية مفهومة توضح الخطوة التالية، ومعرف عملية للمساعدة في الدعم بدل كشف تفاصيل تقنية أو بيانات عميل.
- تحسين الجداول والبحث والفلاتر والتحديد المتعدد، وحفظ التفضيلات غير الحساسة فقط.
- مراجعة RTL والموبايل ولوحة المفاتيح وحالات الفراغ والتحميل وفقد الاتصال.
- مراجعة العبارات التسويقية مثل «معتمد» أو «دقة 100%»؛ لا تستخدم إلا إذا كان هناك دليل مناسب على الادعاء.

**القبول:** سيناريو متكامل ببيانات تجريبية من الاستيراد إلى المتابعة، ومعاينة مرئية للشاشات المتغيرة، واختبار صلاحيات المستخدم العادي مقابل المخزون والأدمن، دون تسريب بيانات فعلية في اللقطات.

## 11. تسليم كل طلب للمراجعة

### قبل التنفيذ — يملؤه Antigravity

```text
معرف الطلب والمرحلة:
مرجع HEAD والملفات المعدلة الموجودة مسبقًا:
المشكلة ودليلها من الكود الحالي:
السلوك المطلوب والسلوك الحالي:
الملفات المزمع تغييرها:
خطوات التنفيذ:
أثر التوافق والبيانات والإصدارات:
اختبارات القبول:
الترحيل والرجوع إن لزم:
القرارات التي تحتاج حسمًا:
المطلوب من Codex: مراجعة فقط وقرار PLAN_APPROVED أو CHANGES_REQUESTED أو BLOCKED.
```

### بعد التنفيذ — يملؤه Antigravity

```text
معرف الطلب ومرجع الخطة المعتمدة:
مرجع التغييرات الفعلية وملخص diff:
الملفات الجديدة والمعدلة:
السلوك الذي تغير:
أوامر الاختبار ونتائجها:
دليل نجاح الحالات التي كانت تفشل:
أي انحراف عن الخطة:
القيود والاختبارات التي لم تنفذ:
المطلوب من Codex: فحص مستقل وقرار REVIEW_PASSED أو CHANGES_REQUESTED أو BLOCKED.
```

### بوابات التحقق الأساسية

```powershell
git status --short
git diff --stat
npm --prefix backend test -- --runInBand
npm --prefix frontend run build
```

هذه الأوامر لا تغني عن اختبارات المرحلة. يفحص المراجع الملفات الجديدة أيضًا لأن `git diff` وحده لا يعرض الملفات غير المتتبعة. لا يتم إجراء اختبارات تكتب في الإنتاج أو ترسل فواتير حقيقية ضمن هذه البوابات.

## 12. إرسال طلب المراجعة إلى Codex

التفاصيل في `AGENTS.md`. يحفظ Antigravity brief كاملًا بترميز UTF-8 في مجلد مراجعة خارج التطبيق، ويختار مجلد نتيجة جديدًا لكل محاولة:

```powershell
node 'C:/Users/GeMy/.gemini/antigravity/skills/codex-delegate/scripts/relay.mjs' --brief '<absolute-brief-path>' --cd 'C:/Users/GeMy/.gemini/antigravity/scratch/FawterX' --read-only --ignore-user-config --out-dir '<unique-output-directory>' --timeout 20m
```

- استبدل القيم بين `< >` بمسارات حقيقية؛ لا تنفذ المثال كما هو.
- اقرأ `result.json` والرد الكامل؛ نجاح تشغيل الأداة ليس قرار اعتماد للخطة.
- الجلسة الناتجة منفصلة عن مهمة Codex المفتوحة. عند المتابعة استخدم `--session` مع `threadId` الصحيح، مع إبقاء `--read-only`.
- أثناء اختبار الإعداد بتاريخ هذه الوثيقة، استدعاء Codex نجح، بينما استدعاء Antigravity CLI تعطل بسبب الحصة. هذه نتيجة اختبار سابقة وليست حكمًا على الحالة وقت التنفيذ.

## 13. أول طلب مطلوب من Antigravity

**ابدأ بالمرحلة الأولى فقط:** افحص جدول صلاحيات المخزون، وأعد إنتاج تجاوز التسوية وتجاوز الصرف عبر استيراد الفواتير باختبارات معزولة، ثم اكتب خطة الإصلاح وأرسلها إلى Codex للمراجعة قبل تعديل كود التطبيق. لا تجمع معها إعادة هيكلة المخزون أو تغيير التوقيع أو النشر.

## 14. تعليمات مباشرة إلى Antigravity: كيف تسلّم إلى Codex بوضوح واختصار

**أنت المنفذ، وCodex المراجع.** المطلوب منك تقليل وقت فهم الطلب دون إخفاء معلومات تؤثر في الحكم. Codex يستطيع قراءة نفس ملفات المشروع؛ لا يحتاج أن تنسخها كلها في رسالتك. لكنه لا يرى محادثتك ولا يعرف تلقائيًا ما اتفقت عليه مع المستخدم.

### قواعد التسليم

1. **طلب واحد وقرار واحد في كل رسالة:** مراجعة خطة، أو مراجعة تنفيذ، أو إعادة مراجعة إصلاحات. لا تخلط عدة مراحل مستقلة.
2. **ابدأ بالنتيجة المطلوبة:** «أريد اعتماد خطة إصلاح تجاوز صلاحية الصرف»، وليس سرد ما قرأته بالتتابع.
3. **ملخص من 150 إلى 300 كلمة عادةً:** زد فقط عند وجود تعقيد ضروري. هذا هدف للاختصار وليس سقفًا يحذف معلومة حاسمة.
4. **اذكر الحالة الحالية في سطرين:** المستخدم المتأثر، الخطوات التي تُظهر المشكلة، وما يحدث بدلًا من السلوك المطلوب.
5. **اذكر نطاقًا محددًا:** ملفات بمسارات مطلقة، وأسماء الدوال أو أسطر الاستدلال، وما إذا كانت جديدة أو معدلة. لا تكتفِ بعبارة «راجعت الباك إند».
6. **افصل الحقيقة عن الاقتراح:** استخدم «أثبت الاختبار»، «ظهر في الكود»، أو «أقترح». لا تقدم توقعًا على أنه تحقق فعلي.
7. **حافظ على سياق المراجعة:** معرف الطلب، مرجع الخطة المعتمدة، `HEAD`، والملفات المعدلة مسبقًا. إذا كانت مراجعة متابعة، أرسل معرف الجلسة السابقة والقرار السابق عند الحاجة.
8. **أرسل الدليل المختصر:** اسم الاختبار والحالة ونتيجته ورمز الخروج. عند الفشل أرفق مقتطف الخطأ المرتبط بالمشكلة ومسار السجل الكامل؛ لا تنسخ مئات أسطر logs.
9. **لا ترسل الملفات كاملة:** أرسل قائمة الملفات ومسار diff أو اجعل Codex يقرأ diff الحالي. انتبه للملفات الجديدة غير المتتبعة؛ وفر مساراتها لأنها لا تظهر في `git diff` المعتاد.
10. **اترك حالة الكود ثابتة أثناء المراجعة:** لا تعدل الملفات التي يراجعها Codex حتى يصل الرد. إذا اضطررت لتعديلها، أخبره وقدم مرجعًا جديدًا؛ الاعتماد القديم لا يغطي تغييرات لاحقة.
11. **اذكر المطلوب حسمه فقط:** إن وجدت بدائل تصميمية، قدم البديلين والفرق المؤثر وترشيحك وسببه. لا ترسل قائمة احتمالات مفتوحة بلا توصية.
12. **اذكر الحدود بصدق:** مثل «لم أجرب التوكن الحقيقي» أو «الاختبارات بمحاكي Firestore». لا تستخدم «تم 100%» أو «كل شيء آمن» بدل الأدلة.
13. **لا تطلب من Codex كتابة الحل:** عند اعتراضه، أنت تصلح الكود والاختبارات ثم تعيد التسليم.
14. **لا ترسل أسرارًا أو بيانات عميل فعلية:** استخدم عينات مجهولة البيانات. لا ترفق `.env` أو مفاتيح الحسابات أو الفواتير الحقيقية.

### نموذج مختصر لمراجعة الخطة

```text
TYPE: PLAN_REVIEW
TASK: FX-001 | المرحلة 1 | منع تجاوز صلاحية الصرف
REPO: C:/Users/GeMy/.gemini/antigravity/scratch/FawterX
BASE: <HEAD الحالي + وصف الملفات المعدلة مسبقًا>

المشكلة: <مستخدم بصلاحيات X يرسل Y فيحصل Z، والمطلوب رفضه قبل أي كتابة>.
الدليل: <المسار/الدالة + اختبار إعادة الإنتاج ونتيجته، أو تصريح بأنه لم ينفذ بعد>.
الخطة: <3 إلى 5 خطوات محددة، تتضمن أين يفرض التحقق وكيف تتوحد القيم>.
الملفات: <المسارات المطلقة + دور كل ملف>.
القبول: <حالتا منع وحالة سماح + أي شرط بيانات ضروري>.
التوافق/المخاطر: <المعلومة المؤثرة أو لا يوجد تغيير مخطط على البيانات>.
التفاصيل: <مسار خطة أطول إن كانت مطلوبة>.

راجع فقط. اقرأ AGENTS.md والملفات ذات الصلة، وأعد PLAN_APPROVED أو
CHANGES_REQUESTED أو BLOCKED مع الأسباب. لا تعدل التطبيق ولا تفوض مراجعًا آخر.
```

### نموذج مختصر لمراجعة التنفيذ

```text
TYPE: IMPLEMENTATION_REVIEW
TASK: FX-001
APPROVED_PLAN: <معرف/مسار الخطة وقرار مراجعتها>
BASE: <HEAD + baseline التغييرات الموجودة مسبقًا>
DIFF: <مسار diff الكامل أو تحديد التغييرات الحالية + مسارات الملفات الجديدة>

النتيجة: <التصرف قبل الإصلاح وبعده في سطرين>.
التغييرات: <حتى 5 نقاط مرتبطة بالملفات/الدوال>.
الاختبارات: <الأوامر، عدد الناجح والفاشل، رموز الخروج، مسارات النتائج>.
دليل الانحدار: <اسم الاختبار الذي يفشل قبل الإصلاح وينجح بعده إن تم إثبات ذلك>.
اختلاف عن الخطة: <اذكر أي اختلاف ولا تخفه>.
لم يختبر: <قيود التحقق>.

الكود ثابت للمراجعة. افحص diff والملفات الجديدة وأعد الفحوص المناسبة مستقلًا.
أعد REVIEW_PASSED أو CHANGES_REQUESTED أو BLOCKED. لا تصلح الكود بنفسك.
```

### نموذج متابعة بعد الملاحظات

```text
TYPE: RE_REVIEW
TASK: FX-001
PREVIOUS_REVIEW: <مسار الرد السابق / threadId>
UPDATED_DIFF: <المرجع الجديد + الملفات التي تغيرت منذ المراجعة>

R1: <الملاحظة> -> <التصحيح: ملف/دالة> -> <اختبار الإثبات ونتيجته>.
R2: <الملاحظة> -> <التصحيح أو سبب الاعتراض مدعومًا بالدليل>.
تغيير إضافي: <إن وجد، لتحديد ما يحتاج مراجعة جديدة>.
المتبقي: <لا شيء أو نقطة محددة>.

راجع التصحيحات وتأثيرها على النطاق الأصلي. إذا فتحت جلسة جديدة بدل استئناف
الجلسة السابقة، ضمّن ملخص المشكلة والخطة المعتمدة وملاحظات المراجع أيضًا.
```

### شكل الرد المطلوب من Codex

```text
VERDICT: <PLAN_APPROVED / REVIEW_PASSED / CHANGES_REQUESTED / BLOCKED>
TASK / REVIEWED_STATE: <معرف الطلب ومرجع الخطة أو التغييرات>
FINDINGS:
- R1 [الأولوية] <المشكلة، دليل الملف/السطر، أثرها، المطلوب من Antigravity>.
VERIFIED: <ما تم فحصه أو تشغيله فعليًا>.
NOT_VERIFIED: <الحدود التي تؤثر في الحكم>.
NEXT: <الإجراء التالي المحدد>.
```

الرد يكون مختصرًا بقدر الإمكان، مع تفاصيل كافية لإصلاح كل ملاحظة. المطلوب تقليل الحشو وتكرار السياق، مع بقاء المراجعة مستقلة ودقيقة.

</div>
